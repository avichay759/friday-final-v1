name: Build FRIDAY APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Initialize Friday Core
        run: echo "Friday Core System is Online and Secure"
        
